@model IEnumerable<Home.Models.Transaction>
@*@model PagedList.IPagedList<Home.Models.Transaction>*@
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
    <script src="~/Scripts/jquery-2.1.3.min.js"></script>
    <script src="~/Scripts/bootstrap.min.js"></script>
    <script src="~/Scripts/jquery.validate.unobtrusive.js"></script>
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    <link href="~/Content/animate.min.css" rel="stylesheet" />

    <style>
        .table th /*.table td*/ { 
     border-top: none !important; 
 }
    </style>
</head>
<body>
    <table class="table">

        @*style="position:fixed;margin-top:-25px;text-decoration:none"*@

        <tr>
            <th>
                Category
            </th>
            <th>
                Transaction
            </th>
            <th>
                Description
            </th>
            <th>
                Amount
                @*@Ajax.ActionLink("Amount", "Create", new { sortOrder = ViewBag.Amount },null)*@
                @*@Html.ActionLink("Amount", "Create", new { sortOrder = ViewBag.Amount })*@
            </th>
            <th>
                Date
                @*@Html.ActionLink("Date", "Create", new { sortOrder = ViewBag.Date })*@
            </th>
            <th>
                Transaction
            </th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Categories.CategoryName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TransactionName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Amount)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DateAction)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TransactionTypes)
                </td>
            </tr>
        }
    </table>


    <script src="~/Scripts/jquery-2.1.3.min.js"></script>
    <script src="~/Scripts/bootstrap.min.js"></script>
    <script src="~/Scripts/bootstrap-notify.min.js"></script>

    @*<div class="panel-footer">
            Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount
            @Html.PagedListPager((PagedList.IPagedList)Model, page => Url.Action("_PartialIndex", new { page }), PagedListRenderOptions.EnableUnobtrusiveAjaxReplacing(PagedListRenderOptions.PageNumbersOnly, new AjaxOptions { InsertionMode = InsertionMode.Replace, HttpMethod = "Get", UpdateTargetId = "tabData" }))
        </div>*@
</body>
</html>
@*@section Scripts {
        @Scripts.Render("~/Scripts/jquery-ui-1.11.4.js")
        @Scripts.Render("~/bundles/jqueryval")
    }*@